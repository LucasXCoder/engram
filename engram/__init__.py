"""Engram — turn a creator's public output into a queryable knowledge-graph "brain".

The Python package holds the shared tooling; a *brain* is just a data directory it operates on.
See the `engram` CLI (`python -m engram --help`) and SKILL.md for the full workflow.
"""

__version__ = "0.1.2"
